import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const EmployeesPage = () => {
  return (
    <SafeAreaView className="flex-1 bg-[#f5f7fb] p-4">
      <Text>Employees Page</Text>
    </SafeAreaView>
  );
};

export default EmployeesPage;
