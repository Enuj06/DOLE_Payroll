import { SafeAreaView } from "react-native-safe-area-context";

const IndexPage = () => {
  return (
    <SafeAreaView className="flex-1 bg-[#f5f7fb] p-4 items-center"></SafeAreaView>
  );
};

export default IndexPage;
